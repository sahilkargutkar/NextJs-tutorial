// this is comknvnvinsnsnvlnvsnsni v

const userName = 1;
console.log(userName);

let userRepo: string;

//to make a tsconfig file just go into the main folder and type in terminal tsc --init
//make sure you are in the right folder
//after creating the tsconfig file just type tsc in terminal

// 2. If the target value in the tsconfig is es5 we can see that the js code after the tsc file is compiled will
// be cahnged to var instead of let and const
